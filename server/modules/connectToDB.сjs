const mongoose = require("mongoose");

const app = async () => {
  await mongoose.connect("mongodb://localhost:27017/network");
  console.log('Подключение к базе данных выполнено');
};

module.exports = app